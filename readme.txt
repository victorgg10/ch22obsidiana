Para ejecutar el servidor de prueba de archivo json para
gurdar usuarios con el registro del login, primero
ingresamos en la consola en git bash:

-$npm i 

despues que se instale lo necesario, ingresamos el siguiente 
comando:

-$npm run json:server 

y ya con esto se pueden guardar y acceder a los datos del 
archivo json